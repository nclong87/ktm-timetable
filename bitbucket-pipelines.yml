
pipelines:
  default:
    - step:
        name: Build and test
        deployment: staging
        image: node:10.16.0
        caches:
          - node
        script: # Modify the commands below to build your repository.
          - rm -rvf node_modules
          - npm install
          - npm run build-staging
        artifacts:
          - staging/**
    - step:
        name: Deploy to Github Pages
        deployment: staging
        image: node:10.16.0
        caches:
          - node
        script:
          - git config --global user.name "ktmtimetable@gmail.com"
          - git config --global user.password "WyViU5M6NLneqTUg"
          - npm run deploy-staging
